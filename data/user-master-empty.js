/* ------------------------------------------------------------------
   USER MASTER — intentionally empty.

   Sign-in runs against the SiteSurveyUsers list in SharePoint through
   the directory flow (authUrl in app-config.js). Keeping this list
   empty is the point: employee codes and passwords stay out of the
   repository entirely.

   Put rows back here only if you want the app to work with no
   directory flow configured.
------------------------------------------------------------------ */
const USER_MASTER = [];
